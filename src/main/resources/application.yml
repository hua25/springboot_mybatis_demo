spring:
  datasource:
    url: jdbc:mysql://localhost:3306/dbgirl?useUnicode=true&characterEncoding=utf-8&useSSL=true
    username: root
    password: "#db_admin"
    driver-class-name: com.mysql.jdbc.Driver
mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.demo.entity